# 4.1.b Daily Scrum Log

*This daily call progress simulates updates from the standup meetings during the critical phases of development (Backend Engine creation and Frontend Visualization integration).*

---

### Week 1: Core Engine Development

**Day 1**
*   **What did I do yesterday?** Set up the project structure (`gsql` folder, React frontend initialization). Installed `spaCy` and initialized Git.
*   **What will I do today?** Work on `schema.py` to define the programmatic `DatabaseSchema`, `Table`, and `Column` classes. Start defining a mock IMDB schema.
*   **Blockers:** None.

**Day 2**
*   **What did I do yesterday?** Finished the schema structure. Successfully mocked the Movies, Actors, and Directors tables with PK/FK relationships.
*   **What will I do today?** Implement the `NLPProcessor` in `nlp.py` using `spaCy` to map incoming text to lemmatized tokens.
*   **Blockers:** Exploring the best way to handle complex compound nouns in `spaCy`.

**Day 3**
*   **What did I do yesterday?** NLP tokenization is working.
*   **What will I do today?** Start `tagger.py`. Need a rule-based system to assign "Meta", "Value", and "Cond" tags based on schema lookup and dictionaries.
*   **Blockers:** None currently. Tagging seems straightforward with a rule dictionary.

**Day 4**
*   **What did I do yesterday?** Implemented `SemanticTagger`. Successfully filtering out stop-words and categorizing keywords dynamically.
*   **What will I do today?** Begin `linker.py` to link tags back to actual schema Tables and Columns.
*   **Blockers:** Need to figure out a method for resolving ambiguous column names across tables.

**Day 5**
*   **What did I do yesterday?** Linked tags to the `DatabaseSchema`.
*   **What will I do today?** The hard part: Graph-based Breadth-First Search (BFS) for Join inference. Will map adjacency based on Foreign Keys.
*   **Blockers:** The BFS logic might be heavy; testing with the 3-table IMDB schema first.

---

### Week 2: Joining, Generation, and API Integration

**Day 6**
*   **What did I do yesterday?** Successfully implemented the BFS algorithm. It can find paths between "Actor" and "Movie" by joining on "Cast".
*   **What will I do today?** Finish `generator.py` to compile the parsed query structure into valid SQL strings.
*   **Blockers:** None.

**Day 7**
*   **What did I do yesterday?** G-SQL core logic is generating functional SQL commands for complex multi-table queries.
*   **What will I do today?** Begin work on `server.py` using **FastAPI** to expose these logic modules to a REST endpoint.
*   **Blockers:** Configuring CORS for local React development.

**Day 8**
*   **What did I do yesterday?** Built the `POST /api/translate` endpoint. It returns the SQL string along with all intermediate tagged token states.
*   **What will I do today?** Shift to the frontend. Implement `QueryInput` and `SchemaViewer` components using React and Vite.
*   **Blockers:** None.

**Day 9**
*   **What did I do yesterday?** UI basics are up. Static schema rendering looks good.
*   **What will I do today?** Implement the `PipelineVisualizer`. I need to logically map the FastAPI token JSON into UI "Chips" with dynamic colors based on their G-SQL Tag assigned in Day 4.
*   **Blockers:** CSS flexbox alignment issues for dynamically wrapping chip arrays.

**Day 10**
*   **What did I do yesterday?** Solved the CSS issues. The React visualizer correctly highlights "Meta" (tables/columns) in blue, and "Values" in green.
*   **What will I do today?** Add error handling, refine the README instructions, code cleanup, and prepare for presentation demo.
*   **Blockers:** Ready for testing.

---
